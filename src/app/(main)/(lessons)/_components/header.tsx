import React from "react";

type HeaderProps = {
  hearts: number;
  percentage: number;
  hasActiveSubscriptions: boolean;
};

const Header = ({
  hearts,
  percentage,
  hasActiveSubscriptions,
}: HeaderProps) => {
  return <div></div>;
};

export default Header;
